# GDS Workbench Release 1 traceability

Status: active

| Phase | Requirement | Primary implementation | Evidence | Status |
|---|---|---|---|---|
| 1 | Canonical `application` schema and 15 normalized tables, including durable exact Workflow Run Object selection and Mapping target Object/source System selection | `database/10_application.sql` | Focused Application governance and Workflow Run database gate: 50 passed; root Mapping/no-op regression: 23 passed. | completed |
| 1 | Nullable web provenance; MCP inserts remain valid | `database/04_model.sql`, `database/05_workflow_analysis.sql`, `database/06_workflow_conceptual.sql`, `database/07_workflow_logical.sql`, `database/08_workflow_dimensional.sql`, `database/09_workflow_mapping.sql` | Included in the 162-pass database gate. | completed |
| 1 | Prompt resolution, immutable versions, frozen run snapshots | `application.prompt_*`, `application.workflow_run*` | Prompt mutations: 13/13 passed; SQL-guide/artifact/workflow-run group: 19 passed. | completed |
| 1 | Structured Mapping templates and stored SQL artifacts | `application.output_template*`, `application.sql_generation_guide*`, `application.generated_sql_artifact` | Output-template modules: 22 passed; SQL-guide modules: 14 passed; included in the 162-pass database gate. | completed |
| 1 | Canonical install, grants, verification, seed, inventory | numbered SQL, database docs, safe seed | Clean install, exact 24-function web allowlist, verifier, seed, eligibility, and provenance checks passed. | completed |
| 2 | Preserve every MCP tool and security boundary | `mcp_server/gds_etl_workbench/` | Refreshed full MCP gate: 633 tests passed; source-scoped strict Pyright and Ruff passed; exact 57-tool/2-prompt/0-resource surface and deterministic App Service ZIP passed. | completed |
| 2 | Canonical scope eligibility without public Model Scope mutation | MCP Model reads, snapshots, Change Set validation/materialization | Disposable PostgreSQL eligibility, scope, mapping, Change Set, and negative public-surface tests included in the 465-pass gate. | completed |
| 2 | Nullable web provenance and safe Databricks boundary | MCP record contracts/materializer and mocked Databricks executor | Inference-only Analysis round trip, null-provenance inserts, recursive forbidden-field schemas, SQL redaction, and mocked executor tests included in the 465-pass gate. | completed |
| 3 | V2 plugin compatibility and complete target readiness | `plugins/v2/gds/` | 87 Python tests passed; 27 PowerShell-runtime tests skipped because PowerShell is unavailable; 39 Node tests passed; plugin validation, Ruff, JavaScript syntax, strict Pyright, runtime-contract, and deterministic checked-in ZIP checks passed. | completed |
| 4 | V2 Workbench prototype-aligned visual language | `plugins/v2/gds/workbench/` | 10 focused visual/DOM contracts, 39 Node interaction tests, responsive and accessibility media contracts, full 86-pass V2 Python gate, 27 unavailable-PowerShell skips, strict Pyright/Ruff/syntax checks, and deterministic final ZIP all passed. | completed |
| 5 | Least-privilege web Model and exact Model Scope mutations | `application.create_model`, `update_model`, `archive_model`, `replace_model_scope` | 13 mutation tests; 65-test focused Application, security, grants, and Workflow Run database gate. | completed |
| 5 | FastAPI backend with direct governed operations | `web_app/backend/` | Tenant/Model/Scope/Lock/Metadata/Prompt/Profiling/Assertions/Conceptual/Logical/Dimensional/Mapping/Code reads, Model Change Sets, bounded agent context, and authoring execution/storage are implemented. Conceptual, Logical, Dimensional, and Mapping support explicit one-shot, tool-assisted, and detailed-coverage authoring with frozen prompts/context, bounded validation/repair, and one transaction-bound finalization into the identical Workflow Run-bound validated draft contract. Mapping additionally freezes one exact target Object/source System pair and verified `mapping.standard@1.0.0` profile, validates complete Header/Attribute coverage and transformation templates, screens provider/persisted JSON for sensitive data, and provides a no-external-I/O local fake for every mode. Its final gate is 191 non-database tests plus 5 disposable-PostgreSQL preparation, execution, atomic draft, Apply, and read tests; malformed execution stores no partial draft or Mapping rows; Ruff and strict Pyright are clean. Tool-assisted execution exposes only a per-run, read-only, bounded in-memory catalog. Zero-change authoring outcomes persist an exact, atomically terminal, revision-stable no-op receipt; changed outcomes atomically validate the draft, append the exact final event, and complete the Run. Their separate explicit Apply path rechecks actor, successful Run state, Model/draft/digest fences, and the owned Tenant Lock; revalidates and materializes atomically; stamps Workflow provenance; advances one revision; and replays exactly from the append-only applied event. Analysis inference updates only inference-owned fields and preserves validation evidence/provenance plus user status/lock. Deterministic Analysis validation executes fixed aggregate-only Databricks queries, binds a DB-computed non-secret connection witness, persists validation-owned fields atomically, and emits bounded progress. Generic/MCP-style Apply retains nullable web provenance. Target-first SQL Code Generation now freezes selected/all coverage, Model revision, plural contributing source Systems, aggregate Mapping/source digests, and exact SQL-guide provenance; it makes one provider call per target, validates SQL-only output, and atomically replaces one `Model + layer + target Object` artifact only after every target succeeds. Stale artifacts remain readable, failed/drifted runs preserve prior SQL, and bounded `.sql`/`.zip` downloads use safe target filenames. Code Generation reads/downloads are now a cohesive feature module, and Logical Entity reads support an effective Submodel membership filter. Durable multi-replica execution now uses PostgreSQL claim/lease/recovery functions, an exact-token claim fence before authoritative writes, heartbeat renewal, safe claim rotation, bounded recovery exhaustion, and a separate worker process packaged from the same backend image; its focused non-database gate is 13 passed. Remaining Excel commands and final modular cleanup remain pending. | in progress |
| 5 | Shared governed Assertion staged-write safety | Canonical Assertion records and Model Change Set staging; FastAPI Assertion reads | MCP and FastAPI now share one recursive normalized-JSON safety contract. Direct and chunk staging reject oversized text/JSON, excessive depth/complexity, and raw-row/prompt/tool/file/secret-shaped keys before database access with value-free errors. The complete Assertion Section is bounded at the documented 4 MiB while every other Model Section retains 16 MiB. No route, table, migration, nullable provenance, or MCP inventory change. Focused gates: 37 MCP Change Set tests, 18 web Change Set/Assertion tests, 6 inventory/schema/prompt/resource checks; touched Ruff and strict Pyright passed. The checked-in V2 contract and current distribution ZIP match the exact 57-tool/2-prompt runtime fingerprint; the focused contract/package gate passed 108 tests. | completed |
| 5 | Complete authorized Metadata catalog and governed operational XLSX export | `GET /api/v1/tenants/{tenant_id}/metadata/datasets`, per-dataset canonical field schema, generic dataset rows, Object detail, `POST .../exports/xlsx`, `metadata.py`, `metadata_repository.py`, `metadata_workbook.py` | Backend exposes the exact 8 Reference, 5 Foundational, and 16 Operational normalized datasets. Tenant authorization precedes every read; Foundational rows use the authorized visibility closure; Connection values remain absent; discovery scope remains the canonical ownership rule. Per-dataset detail exposes only the ordered canonical Pydantic field schema needed for non-guessing Add/Edit forms. Operational-only XLSX export retains its strict manifest and bounds. Governed shared staging now validates from the original JSON representation so strict canonical `date`/`date-time` fields work identically for MCP and web without weakening other types. Focused MCP stage gate: 15 passed; focused web Metadata/Change Set/import gate: 26 passed; Ruff and strict changed-surface Pyright passed. Disposable-PostgreSQL repository recheck is pending because Docker could not start during the latest root run; the prior combined backend gate was 42 passed. The complete Metadata frontend is implemented and its full gate passed 73 tests, typecheck, and production build; backend change-set history/list remains an honest non-blocking UI limitation. | in progress |
| 5 | Tenant-authorized Mapping Output Template catalog | `features/output_templates/`; `GET /api/v1/tenants/{tenant_id}/output-templates` and detail; Mapping run configuration | Read-only active/target-type catalog uses Tenant Read authorization, bounded signed paging, canonical schema-digest verification, and ordered typed fields. It exposes no actor metadata or field example payloads. Focused/neighbor gate: 51 passed; restricted web-runtime disposable-PostgreSQL integration: 1 passed; Ruff and strict Pyright passed. The Model-context Mapping run dialog now loads the active Object and Attribute catalogs separately, preserves independent free-form null choices, shows schema validity, disables invalid schema choices, and sends the exact selected IDs through the existing run request. Full frontend gate: 76 tests plus typecheck and production build passed. | completed |
| 5 | Durable multi-replica Workflow Run execution | `application.claim_next_workflow_run`, `renew_workflow_run_claim`, `release_workflow_run_claim`, `assert_workflow_run_claim`; `features/workflows/execution/`; `workflow_worker.py` | Exact PostgreSQL lease/recovery contract, internal token-redacted claim DTO, typed dispatcher, heartbeat worker, claim-repository integration, and removal of FastAPI process-local background execution are implemented. The shared worker runtime assembles and dispatches Profiling, Analysis inference/validation, Conceptual, Logical, Dimensional, Mapping, and Code Generation; every authoritative execution path receives the exact claim token and uses the common transaction-bound claim fence. Safe graceful shutdown releases only its exact live claim. Focused worker/configuration/repository gate: 13 passed; prior claim/fence and disposable-PostgreSQL gates passed with Ruff and strict Pyright. | completed |
| 5 | React/TanStack frontend matching approved prototype | `web_app/frontend/` | Tenant chooser/Home, Model ledger/overview, read-only Scope, Profiling, Analysis review, Assertions review, Conceptual, Logical, Dimensional, Mapping review/details, target-first stored SQL Code Generation, and Prompt library/Model prompt assignment screens exist. Tenant Lock acquire, renew/extend, confirmed owner release, authorized override/revoke, and bounded history are feature-owned under `features/tenant_locks/`. TanStack Forms mirror bounded duration/purpose/reason feedback; the API sends no client authority; server action flags alone control mutations; successful mutations refetch server-owned Home state without automatic lock actions. Release requires separate confirmation and an exact no-body POST. Override warns it only force-releases, requires a nonblank reason up to 2,000 characters, and returns unlocked so Acquire remains separate. Every authorized Home reader receives an explicit View history control independent of mutation flags; no history request occurs before opening; fixed 50-event pages load only through explicit Load more; repeated cursors stop; event reasons are capped at 2,000 characters; raw errors stay hidden. Full frontend gate: 110 tests, typecheck, and production build passed. Release audit still requires Model create/settings/archive; Scope Add/Remove through the existing web-only governed `application.replace_model_scope` command; Analysis execution and row lifecycle; shared Workflow Run events/draft review/Apply; Assertion authoring; per-run prompt overrides; Mapping coverage/history; SQL guide/ZIP/history. These are implementation blockers, not deferred polish. | in progress |
| 5 | Industry-standard modular web application | feature-owned frontend data/route modules; feature-owned backend Section packages | Architecture deletion-test audit completed. Session, Tenant entry, Tenant Lock operations, Model reads/commands, Model Scope reads, Assertions, Conceptual reads, Logical reads, Dimensional reads, Mapping reviews, Prompt Library, Workflow Overview, Workflow Runs, and Workflow Commands are now canonical under `features/session/`, `features/tenants/`, `features/tenant_locks/`, `features/models/`, `features/model_scope/`, `features/assertions/`, `features/conceptual/`, `features/logical/`, `features/dimensional/`, `features/mapping/`, `features/prompts/`, `features/workflows/overview/`, `features/workflows/runs/`, and `features/workflows/commands/`. Workflow Overview owns its contracts, router, service Protocols, unchanged seven-row ledger SQL/PostgreSQL implementation, Tenant Read authorization, Model-not-found error, derived states, and prerequisite warning rules. Workflow Runs owns its contracts, router, service Protocols, unchanged run/detail/event SQL and PostgreSQL implementation, filter-bound signed cursor pagination, bounded event reads and SSE reconnect behavior, Tenant Read authorization, existing DTO redaction, draft linkage, and safe errors. Workflow Commands owns its request/result contracts, router, service Protocols, unchanged governed creation SQL and PostgreSQL implementation, server-derived Principal authorization, Tenant Lock and Model revision fences, idempotency correlation, agent/prompt selection validation, state-transition result, and value-safe error mapping. Conceptual reads now share the existing `features/conceptual/` module with authoring through distinct `read_contracts.py`, `read_service.py`, and `read_router.py`; exact DTOs, seven SQL queries, filter-bound signed paging, Tenant Read authorization, bounded Object/Assertion Support normalization, routes, and safe errors are unchanged, while authoring implementation remains untouched. Logical reads likewise share `features/logical/` through distinct read modules; exact DTOs, thirteen SQL queries, filter-bound signed paging, Entity/Submodel membership filtering, Tenant Read authorization, bounded Object/Attribute/Assertion source normalization, routes, safe errors, and Dimensional shared-contract imports are unchanged, while Logical authoring and policy implementation remain untouched. Dimensional reads now similarly use `features/dimensional/read_contracts.py`, `read_service.py`, and `read_router.py`; exact DTOs, ten SQL queries, filter-bound signed paging, Logical linkage, Tenant Read authorization, bounded Object/Attribute/Assertion source normalization, routes, and safe errors are unchanged, while Dimensional authoring and policy implementation remain untouched. Mapping reviews now similarly use `features/mapping/read_contracts.py`, `read_service.py`, and `read_router.py`; exact DTOs, six SQL queries, filter-bound signed paging, bounded JSON/provenance validation, Tenant Read authorization, routes, safe errors, and Code Generation reference linkage are unchanged, while Mapping authoring remains untouched. Mapping preparation now owns validated contracts and Protocols in `preparation_contracts.py`, the byte-identical eight SQL queries plus PostgreSQL Run-plan/context repository adapters in `preparation_repository.py`, and authorization/readiness rules in `readiness.py`; the 2,418-line catch-all was deleted without a shim while public package imports and behavior remain unchanged. Prompt Library now uses `features/prompts/contracts.py`, `service.py`, and `router.py`; exact DTOs, thirteen SQL queries, allowed-variable and placeholder behavior, Prompt version and Model assignment semantics, filter-bound signed paging, Tenant Read/Tenant Model Write/Super Admin authorization, Tenant Lock and timestamp/ID fences, ten routes, and safe error mapping are unchanged. Session retains its exact authenticated `/api/v1/session` behavior; the independent agent-capabilities route remains explicitly composed in `main.py`. Tenant Lock, Models, Model Scope, and Assertions retain their governed behavior. Runtime, app composition, tests, and error consumers import the feature packages; fifteen obsolete root modules were deleted without compatibility shims. Routes, DTOs, policies, SQL, and behavior are unchanged. Latest local-fake/authoring-neighbor gate: 132 passed; nine governed disposable-PostgreSQL cases collected without execution because Docker remained unavailable; touched Ruff format/check and strict Pyright passed; the full 177-module import-cycle check passed. No external system was called. Frontend HTTP defaults, bounded 16-KiB JSON error parsing, `ApiError`, and typed success-response readers now have one private `core/http.ts` implementation. `features/tenant_locks/api.ts` owns its interface plus acquire/renew/release/override/history transports. The cohesive frontend `features/tenants/api.ts` module owns Session bootstrap, Tenant list/selection, and Tenant Home contracts and transports through `createTenantsApi(request)`; a shallow one-route Session adapter was deliberately avoided. Frontend `features/models/api.ts` owns Model ledger/detail/overview contracts and exact list/detail/overview transports through `createModelsApi(request)`; no Model command transport existed in the frontend root to move. Frontend `features/model_scope/api.ts` owns Model Scope filters, records, pages, details, exact natural-key normalization, page defaults, and list/detail transports through `createModelScopeApi(request)`; no mutation was invented. Frontend `features/metadata/api.ts` owns Metadata registry/detail/rows, XLSX export/import, and Metadata Change Set contracts and exact transports through `createMetadataApi(request)` with only a `core/http.ts` dependency. Frontend `features/workflows/api.ts` owns shared agent capabilities/selection, Model Workflow/execution/run-state vocabulary, Workflow Run filters/records/details/events/commands, and exact list/read/event/general-create plus explicit execute transports through `createWorkflowsApi(request)`, with only a `core/http.ts` dependency. Frontend `features/profiling/api.ts` and `features/analysis/api.ts` own their filters, DTOs, query keys, and exact read transports; Profiling also owns its distinct create-run transport. Their UI Interfaces compose owned Workflow and Model Scope contracts without root imports; shared `ReviewStatus`, Workflow-owned run-state presentation, and shared required-date formatting remove the Analysis-to-Profiling dependency while preserving rendering. Frontend `features/assertions/api.ts` owns Modeling Assertion contracts, filters, pages, details, query keys, exact document/record read transports, and natural-key normalization. Assertions UI imports only owned/shared contracts; recursive `JsonValue`/`JsonObject` live in `shared/contracts.ts` for both Assertions and Mapping, preserving normalized rendering without a feature dependency. Frontend `features/conceptual/api.ts`, `features/logical/api.ts`, and `features/dimensional/api.ts` own their DTOs, filters, pages/details, query keys, and exact normalized read transports; their UI Interfaces compose owned Workflow and Model Scope Interfaces without root imports. Logical retains its bounded 250-page, revision-consistent, repeated-cursor-protected Submodel option loader; shared modeling confidence/cardinality vocabulary avoids modeled-feature dependencies. Frontend `features/prompts/api.ts` now owns all Prompt DTOs, filters, commands, query keys, its ten-method transport Interface, exact natural-key path normalization, assignment-version loader, and `createPromptsApi(request)`. Prompt production UI imports feature-owned types and shared core/Models/Workflow Interfaces without a root-client dependency; exact adapter characterization now lives beside the Module. Frontend `features/mapping/api.ts` owns Mapping review and Output Template contracts, filters, pages/details, query keys, its six-method transport Interface, exact natural-key path normalization, and `createMappingApi(request)`. Its Output Template loader is capped at five pages per target type; its Mapping Scope loader is capped at 250 revision-consistent pages with repeated-cursor protection. Mapping production UI imports feature-owned and shared core/Models/Scope/Workflow Interfaces without a root-client dependency. Frontend `features/code_generation/api.ts` owns Code Generation filters, targets, pages/details, provenance DTOs, query keys, its two-method transport Interface, exact natural-key path normalization, and `createCodeGenerationApi(request)`. Its UI composes owned Mapping, Models, and Workflow Interfaces without a root-client dependency; the direct bounded `.sql` download path remains unchanged. The root client composes all fifteen feature adapters and preserves its existing type and `ApiError` exports; the duplicate root Mapping and Code Generation transport characterizations were deleted. Exact modeled adapter/UI/full-client characterization: 53 passed. Exact Prompt adapter/UI gate: 7 passed. Exact Mapping adapter/UI/root gate: 43 passed. Exact Code Generation adapter/UI/root/Workflow gate: 24 passed. A focused frontend static architecture guard now resolves the production relative-import graph, rejects feature-to-root-client and core/shared-to-feature dependencies, requires root `api.ts` to remain re-exports plus one empty aggregate Interface and one spread-only client composer, and detects cycles without line-count or route-composition rules. Focused architecture gate: 4 passed. Final full frontend gate: strict typecheck, 133 tests, and production build passed. The workflow-owned Agent execution Interface, immutable request/result/local-tool contracts, dispatch validation, and safe error translation now live inward in `features/workflows/authoring/agent_execution.py`; LangChain Create Agent, OpenAI Agents SDK, and the deterministic local fake remain three outward Adapters. The obsolete integration-owned contract module was deleted without a compatibility shim. The proven 3,590-line local-fake Agent catch-all now uses the small `integrations/agents/composition.py` Module plus cohesive `fake_shared.py`, `fake_conceptual.py`, `fake_logical.py`, `fake_dimensional.py`, and `fake_mapping.py` Modules; the exact Agent execution Interface, dispatch/mode checks, deterministic candidates, tool-call counts, bounded validation, and no-echo safety are unchanged, and `runtime.py` was deleted without a shim. Exact focused gate: 122 passed; touched Ruff format/check and strict Pyright passed. The authorized Metadata catalog now owns its contracts, router, database behavior, fixed SQL repository, and canonical XLSX implementation under `features/metadata/`; composition, Model Scope, Metadata Change Sets, and tests import that feature-owned seam, and the three obsolete root Metadata modules were deleted without shims. Metadata characterization: 54 passed with four disposable-PostgreSQL cases deselected; Scope neighbor gate: 6 passed with its disposable-PostgreSQL case deselected; touched strict Pyright passed. All six relevant disposable-PostgreSQL checks were attempted but could not start their fixture container because Docker was unavailable. Full module import-cycle/stale-reference checks passed (164 modules). A focused static deletion guard now pins the 21 proven retired root feature modules plus the Mapping preparation and local-fake Agent catch-alls, permits feature-to-provider dependencies only at the explicit Workflow runtime assembly seam, keeps the workflow-owned Agent port inward, and checks the complete backend import graph for cycles without file-count or size heuristics. Final gate: static guard 1 passed; full non-database web-backend suite 734 passed with seven disposable-PostgreSQL cases deselected; aggregate Ruff check, touched Ruff format, aggregate strict Pyright, stale-reference scan, 178-module import smoke, and diff check passed. Mechanical public names replaced cross-module private imports; SQL and behavior were unchanged. No external system was called. Backend Section review/authoring locality and repeated Agent Run Selection remain subsequent behavior-preserving slices. Review artifact: `/private/tmp/architecture-review-20260824-190640.html`. | in progress |
| 5 | Profiling/Analysis feature boundary correction | `features/profiling/`; `features/analysis/read_*`; backend composition and runtime imports | Profiling now owns its read contracts/router/service, deterministic planner, Databricks execution Adapter boundary, and governed Workflow lifecycle under `features/profiling/`; Analysis finding review now lives beside Analysis authoring/validation under `features/analysis/read_*`. Main/runtime compose separate read Interfaces with unchanged routes and the same database/signing key; Workflow assembly and the Databricks runtime import the canonical feature modules. The three obsolete root Profiling/Analysis modules were deleted without shims. Focused characterization: 35 passed; neighboring Workflow/Analysis/runtime gate: 61 passed; touched Ruff format/check and strict Pyright passed; full import-cycle check passed across 178 modules; stale-reference scan was clean; all seven moved SQL constants were byte-identical to the pre-move bytecode. The disposable-PostgreSQL Profiling repository check was attempted but its fixture container could not start because Docker was unavailable. No Databricks or external provider was called. | complete |
| 5 | Frontend stylesheet Module boundary | ordered `src/styles.css` manifest; ten feature-owned stylesheets under `src/styles/` | The former 5,994-line catch-all stylesheet is now an ordered ten-import manifest over Foundation, Models/Scope, Analysis/Assertions/Modeled Sections, Profiling, Metadata, Tenant Entry, Tenant Workspace, Model Workspace Overrides, Code Generation, and Prompts Modules. No selector or declaration changed: concatenating the ten files is byte-identical to the prior stylesheet, and the production asset remains `index-BWT8CWfh.css`. A focused manifest test pins exact cascade order and rejects declarations in the manifest. Full frontend gate: typecheck, 122 tests, and production build passed. | complete |
| 6 | Local containers and end-to-end release proof | backend/frontend Dockerfiles, safe local Compose/runner, Azure Container Apps contract | Static packaging contract gate: 24 tests. Fresh backend, worker, frontend, and fixture-created disposable PostgreSQL stack passed health/proxy smoke; real React + FastAPI browser coverage passed across Tenant, Metadata, Model workflow, Prompt, Mapping, and Code Generation routes. Exact generated images, containers, volume, and networks were removed afterward. | completed |

Baseline: the earlier broad run reached 368 passed. The final Phase 1 database
gate reached 162 passed with three expected Phase 2 compatibility failures:
the Excel loader and two MCP Model round trips still use the old Model contract.
